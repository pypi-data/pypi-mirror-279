[tool.poetry]
name = "campus-cli"
version = "0.2.1"
description = ""
authors = ["gazaboo <florian.dadouchi@gmail.com>"]
readme = "README.md"
packages = [{ include = "campus_cli" }]

[tool.poetry.dependencies]
python = "^3.11"
nbformat = "^5.10.4"
click = "^8.1.7"
textract = "^1.6.5"
requests = "^2.32.3"
click-completion = "^0.5.2"

[tool.poetry.scripts]
campus-cli = "campus_cli.cli:cli"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
