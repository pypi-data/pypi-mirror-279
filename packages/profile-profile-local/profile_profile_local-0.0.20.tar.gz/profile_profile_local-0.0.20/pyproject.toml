# This file should be in the future instead of setup.py
# https://python-poetry.org/docs/pyproject

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.poetry]
name = "profile-profile-local"
version = "0.0.11" # # https://pypi.org/project/profile-profile-local
description = "Profile Profile Local Python Package"
readme = "README.md"
authors = [
    "Circlez.ai <info@circlez.ai>",
]