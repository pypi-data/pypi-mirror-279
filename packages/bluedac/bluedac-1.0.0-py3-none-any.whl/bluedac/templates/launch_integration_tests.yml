    stage: deploy
    image: alpine:latest
    script:
        - apk add --update python3 py3-pip
        - python -m venv .venv
        - source .venv/bin/activate
        - python -m pip install -r requirements.txt
        - export INT_TEST_STACK=testing
        - python -m pytest --cov=resources tests/integration/*
