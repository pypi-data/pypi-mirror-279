[tool.poetry]
name = "the-utility-belt"
version = "0.0.1"
description = ""
authors = ["Peter Daly <peter.daly@bark.com>"]
readme = "README.md"
packages = [{include = "the_utility_belt"}]

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
