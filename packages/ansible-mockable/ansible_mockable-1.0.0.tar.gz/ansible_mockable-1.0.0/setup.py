from setuptools import setup, find_packages

setup(
    name="ansible_mockable",
    version="1.0.0",
    description="Ansible Mockable",
    author="Kirill Satarin (@kksat)",
    packages=find_packages(),
)
