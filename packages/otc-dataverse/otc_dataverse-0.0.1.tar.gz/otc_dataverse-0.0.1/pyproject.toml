[project]
name = "otc-dataverse"
version = "0.0.1"
authors = [
  { name="Petar Kasapinov", email="pkasapinov@otcfin.com" },
]
description = "A package for interacting with the Dataverse API"
readme = "README.md"
requires-python = ">=3.8"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"