from setuptools import setup

setup(name='survesh-distributions',
      version='0.1',
      description='Gaussian distributions',
      packages=['survesh-distributions'],
      author='Survesh',
      zip_safe=False)
