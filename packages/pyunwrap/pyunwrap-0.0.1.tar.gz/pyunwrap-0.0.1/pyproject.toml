[build-system]
requires = ["setuptools>=42", "wheel", "pybind11"]
build-backend = "setuptools.build_meta"

[project]
name = "pyunwrap"
version = "0.0.1"
optional-dependencies = { dev = ["pybind11"] }
