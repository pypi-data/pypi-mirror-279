from setuptools import setup, find_packages

setup(
    name='autokeystroke',
    version='0.0.5',
    packages=find_packages(),
    install_requires=[
        # Add dependencies here
    ],
)