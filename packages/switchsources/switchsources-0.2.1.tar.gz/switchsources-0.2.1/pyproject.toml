[tool.poetry]
name = "switchsources"
version = "0.2.1"
description = "A tool to switch sources"
authors = ["Dragonchu"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
typer = "^0.12.3"
inquirer = "^3.2.4"
platformdirs = "^4.2.2"

[tool.poetry.scripts]
switchsources = "switchsources.app:main"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
