[Unit]
Description=Wireguard Controller for Network ${network_name}
After=network.target

[Service]
Type=simple
ExecStart=/etc/wireguard/${network_name}/control/start
ExecStop=/etc/wireguard/${network_name}/control/stop

[Install]
WantedBy=multi-user.target
