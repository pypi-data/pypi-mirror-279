#
# Developed by cKnowledge.org and cTuning.org
#

[build-system]
build-backend = "setuptools.build_meta"
requires = ["setuptools", "wheel"]

[project]
name = "cm4research"

authors = [
    {name = "Grigori Fursin", email = "gfursin@cknowledge.org"}
]

description = "TBD"

requires-python = ">=3.8"

dependencies = [
    "pyyaml",
    "requests",
    "setuptools",
    "cmind"
]

keywords = [
    "cmind",
    "cm4research",
    "automation",
    "portability",
    "modularity",
    "cknowledge",
    "ctuning",
    "mlcommons",
    "research"
]

license = {text = "Apache Software License (Apache 2.0)"}

classifiers = [
    "Intended Audience :: Developers",
    "Intended Audience :: End Users/Desktop",
    "Intended Audience :: Science/Research",
    "Intended Audience :: Information Technology",
    "Topic :: Software Development",
    "Topic :: System",
    "Topic :: Scientific/Engineering",
    "License :: OSI Approved :: Apache Software License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]

dynamic = ["readme", "version"]

[project.urls]
Homepage = "https://github.com/mlcommons/ck"
Source = "https://github.com/mlcommons/ck"

[tool.setuptools]
zip-safe = false

[tool.setuptools.packages.find]
where = ["."]
include = ["cm4research", "cm4research.*"]
exclude = ["docs", "docs.*", "tests", "tests.*"]

[tool.setuptools.package-data]
cm4research = [
  "internal/**/*"
]

#[tool.setuptools.package-data]
#"cm4research.internal" = ["*.yaml", "*.json", "*.md"]

[tool.setuptools.dynamic]
readme = {file = "README.md", content-type = "text/markdown"}
version = {attr = "cm4research.__version__"}
